import{_ as o,c as e,o as t,Q as n}from"./chunks/framework.6f7d7588.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"docs/interview/安全.md","filePath":"docs/interview/安全.md","lastUpdated":1757920371000}'),r={name:"docs/interview/安全.md"},s=n('<h3 id="cookie-和-token-都存放在-header-中-为什么不会劫持-token" tabindex="-1">cookie 和 token 都存放在 header 中，为什么不会劫持 token <a class="header-anchor" href="#cookie-和-token-都存放在-header-中-为什么不会劫持-token" aria-label="Permalink to &quot;cookie 和 token 都存放在 header 中，为什么不会劫持 token&quot;">​</a></h3><p>CSRF 攻击的原因是浏览器会自动带上 cookie，而不会带上 token；</p><p>以 CSRF 攻击为例： cookie：用户点击了链接，cookie 未失效，导致发起请求后后端以为是用户正常操作，于是进行扣款操作； token：用户点击链接，由于浏览器不会自动带上 token，所以即使发了请求，后端的 token 验证不会通过，所以不会进行扣款操作；</p><p>因为传统的 cookie 保存的 session id，服务器会根据这个 session id，确保服务器与客户端对话；这是的 cookie 是有状态的，意味着验证记录或者会话需要一直在服务端和客户端保持。而 token 是无状态的，服务器不记录哪些用户登录了或者哪些 JWT 被发布了，只判断 token 是否有效，通常我们都会给 token 设置有效时间，来确保不被劫持。所有劫持 cookie 比劫持 token，更有效，毕竟 cookie 在某种情况下可以一直使用下去，而 token 不行。</p><h3 id="什么是-xss-攻击-如何防范-xss-攻击" tabindex="-1">什么是 XSS 攻击？如何防范 XSS 攻击？ <a class="header-anchor" href="#什么是-xss-攻击-如何防范-xss-攻击" aria-label="Permalink to &quot;什么是 XSS 攻击？如何防范 XSS 攻击？&quot;">​</a></h3><p>XSS 攻击指的是<strong>跨站脚本攻击</strong>，是一种<strong>代码注入攻击</strong>。攻击者通过在网站注入恶意脚本，使之在用户的浏览器上运行，从而盗取用户的信息如 cookie 等。</p><p><strong>XSS 的本质是因为网站没有对恶意代码进行过滤，与正常的代码混合在一起了，浏览器没有办法分辨哪些脚本是可信的，从而导致了恶意代码的执行。</strong></p><p>XSS 一般分为存储型、反射型和 DOM 型。</p><p>存储型指的是恶意代码提交到了网站的数据库中，当用户请求数据的时候，服务器将其拼接为 HTML 后返回给了用户，从而导致了恶意代码的执行。</p><p>反射型指的是攻击者构建了特殊的 URL，当服务器接收到请求后，从 URL 中获取数据，拼接到 HTML 后返回，从而导致了恶意代码的执行。</p><p>DOM 型指的是攻击者构建了特殊的 URL，用户打开网站后，js 脚本从 URL 中获取数据，从而导致了恶意代码的执行。</p><p><strong>XSS 攻击的预防可以从两个方面入手，一个是恶意代码提交的时候，一个是浏览器执行恶意代码的时候。</strong></p><p>对于第一个方面，如果我们<strong>对存入数据库的数据都进行的转义处理</strong>，但是一个数据可能在多个地方使用，有的地方可能不需要转义，由于我们没有办法判断数据最后的使用场景，所以直接在输入端进行恶意代码的处理，其实是不太可靠的。</p><p>因此我们可以从浏览器的执行来进行预防，一种是使用纯前端的方式，<strong>不用服务器端拼接后返回</strong>。另一种是<strong>对需要插入到 HTML 中的代码做好充分的转义</strong>。对于 DOM 型的攻击，主要是前端脚本的不可靠而造成的，我们对于数据获取渲染和字符串拼接的时候应该对可能出现的恶意代码情况进行判断。</p><p>还有一些方式，<strong>比如使用 CSP</strong>，CSP 的本质是建立一个白名单，告诉浏览器哪些外部资源可以加载和执行，从而防止恶意代码的注入攻击。</p><p>还可以对一些敏感信息进行保护，比如 cookie 使用 http-only ，使得脚本无法获取。也可以使用验证码，避免脚本伪装成用户执行一些操作。</p><h3 id="什么是-csrf-攻击-如何防范-csrf-攻击" tabindex="-1">什么是 CSRF 攻击？如何防范 CSRF 攻击？ <a class="header-anchor" href="#什么是-csrf-攻击-如何防范-csrf-攻击" aria-label="Permalink to &quot;什么是 CSRF 攻击？如何防范 CSRF 攻击？&quot;">​</a></h3><p>CSRF 攻击指的是<strong>跨站请求伪造攻击</strong>，攻击者诱导用户进入一个第三方网站，然后该网站向被攻击网站发送跨站请求。如果用户在被攻击网站中保存了登录状态，那么攻击者就可以利用这个登录状态，绕过后台的用户验证，冒充用户向服务器执行一些操作。</p><p><strong>CSRF 攻击的本质是利用了 cookie 会在同源请求中携带发送给服务器的特点，以此来实现用户的冒充。</strong></p><p>一般的 CSRF 攻击类型有三种：</p><p>第一种是 GET 类型的 CSRF 攻击，比如在网站中的一个 img 标签里构建一个请求，当用户打开这个网站的时候就会自动发起提交。</p><p>第二种是 POST 类型的 CSRF 攻击，比如说构建一个表单，然后隐藏它，当用户进入页面时，自动提交这个表单。</p><p>第三种是链接类型的 CSRF 攻击，比如说在 a 标签的 href 属性里构建一个请求，然后诱导用户去点击。</p><p>CSRF 可以用下面几种方法来防护：</p><p>第一种是<strong>同源检测的方法</strong>，服务器<strong>根据 http 请求头中 origin 或者 referer 信息来判断请求是否为允许访问的站点，从而对请求进行过滤。</strong></p><p>第二种方法是<strong>使用 CSRF Token 来进行验证</strong>，服务器向用户返回一个随机数 Token ，当网站再次发起请求时，在请求参数中加入服务器端返回的 token ，然后服务器对这个 token 进行验证。这</p><p>第三种方式使用<strong>双重 Cookie 验证的办法</strong>，服务器在用户访问网站页面时，向请求域名注入一个 Cookie，内容为随机字符串，然后当用户再次向服务器发送请求的时候，从 cookie 中取出这个字符串，添加到 URL 参数中，然后服务器通过对 cookie 中的数据和参数中的数据进行比较，来进行验证。</p>',27),p=[s];function a(i,c,k,S,d,g){return t(),e("div",null,p)}const l=o(r,[["render",a]]);export{h as __pageData,l as default};
